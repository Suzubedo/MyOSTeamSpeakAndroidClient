pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // ts3j is published via JitPack
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "TS3Client"
include ':app'
